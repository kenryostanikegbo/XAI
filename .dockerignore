.venv/
notebooks/
__pycache__/
*.pyc
.claude/
.git/
.gitignore
.dockerignore
*.docx
*.pdf
*.lock
# GridSearchCV diagnostic artifacts — not loaded by the dashboard; excluding
# drops the Docker image by ~92 MB.
outputs/models/*_search.joblib
# Raw OULAD CSVs are not used at runtime.
data/raw/
# Git LFS stores pointer files in GitHub; Render must receive the real model
# blobs in the Docker build context, not just the pointers.
.gitattributes
